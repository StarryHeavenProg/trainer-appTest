<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">

    <changeSet id="1-main_enums" author="astarell">
        <sql>
            CREATE TYPE user_roles AS ENUM ('APP_USER', 'APP_EXPERT', 'APP_ADMIN');
            CREATE TYPE task_types AS ENUM ('SINGLE_CHOICE', 'MULTIPLE_CHOICE', 'ERROR_FINDING', 'OPEN_ANSWER', 'SQL_QUERY');
            CREATE TYPE task_attempts_status AS ENUM ('REVIEW', 'COMPLETED', 'FAILED');
        </sql>
    </changeSet>

    <changeSet id="2-users-table" author="astarell">
        <sql>
            CREATE TABLE IF NOT EXISTS users (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                first_name VARCHAR(255) NOT NULL,
                last_name VARCHAR(255) NOT NULL,
                email VARCHAR(255) UNIQUE NOT NULL,
                user_role user_roles NOT NULL,
                password_hash VARCHAR(255) NOT NULL,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
        </sql>
    </changeSet>

    <changeSet id="3-tasks-table" author="astarell">
        <sql>
            CREATE TABLE IF NOT EXISTS tasks (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                task_type task_types NOT NULL,
                config JSONB NOT NULL,
                created_by UUID NOT NULL,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
        </sql>
    </changeSet>

    <changeSet id="4-trainers-table" author="astarell">
        <sql>
            CREATE TABLE IF NOT EXISTS trainers (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                name VARCHAR(255) NOT NULL,
                created_by UUID NOT NULL,
                created_at TIMESTAMPTZ DEFAULT NOW()
            );
        </sql>
    </changeSet>

    <changeSet id="5-tasks_trainers-table" author="astarell">
        <sql>
            CREATE TABLE IF NOT EXISTS tasks_trainers (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                task_id UUID NOT NULL,
                trainer_id UUID NOT NULL,
                FOREIGN KEY (task_id) REFERENCES tasks (id) ON DELETE CASCADE,
                FOREIGN KEY (trainer_id) REFERENCES trainers (id) ON DELETE CASCADE,
                UNIQUE(task_id, trainer_id)
            );
        </sql>
    </changeSet>

    <changeSet id="6-task_attempts-table" author="astarell">
        <sql>
            CREATE TABLE IF NOT EXISTS task_attempts (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                task_id UUID NOT NULL,
                user_id UUID NOT NULL,
                user_answer JSONB NOT NULL,
                points DECIMAL,
                status task_attempts_status NOT NULL,
                created_at TIMESTAMPTZ DEFAULT NOW(),
                FOREIGN KEY (task_id) REFERENCES tasks_trainers (id) ON DELETE CASCADE,
                FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE
            );
        </sql>
    </changeSet>

</databaseChangeLog>
